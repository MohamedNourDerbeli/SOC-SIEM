# ===================================================================
#       Advanced OpenSource SIEM - Environment Configuration
# ===================================================================
#
# INSTRUCTIONS:
# 1. For a quick, single-host setup, use it as is.
#
# 2. For a multi-host, production-like setup, change the IP addresses
#    and hostnames to match your environment.
#
# 3. It is STRONGLY recommended to change the default passwords.
#
# ===================================================================

# --- Network Configuration (Default: Single-Host Setup) ---
# All services will run on the same machine (localhost).
# For a multi-host setup, change these IPs.
WAZUH_INDEXER_IP=127.0.0.1
WAZUH_MANAGER_IP=127.0.0.1
GRAYLOG_SERVER_IP=127.0.0.1
GRAFANA_SERVER_IP=127.0.0.1

# --- Hostnames for Certificate Generation ---
# These hostnames will be embedded in the TLS certificates.
# For a single-host setup, 'localhost' is sufficient.
WAZUH_INDEXER_HOSTNAME=wazuh.indexer
WAZUH_MANAGER_HOSTNAME=wazuh.manager
GRAYLOG_SERVER_HOSTNAME=graylog.server
WAZUH_DASHBOARD_HOSTNAME=wazuh.dashboard

# --- Wazuh Component URLs & Credentials ---
WAZUH_INDEXER_URL=https://wazuh.indexer:9200
WAZUH_INDEXER_USERNAME=admin
# Default password generated for security. Change this for production.
WAZUH_INDEXER_PASSWORD=ChangeMe_WazuhIndexer_$(openssl rand -hex 12 )

# Password for the internal 'kibanaserver' user, used by the dashboard.
WAZUH_KIBANASERVER_PASSWORD=ChangeMe_KibanaUser_$(openssl rand -hex 12)

WAZUH_MANAGER_URL=https://wazuh.manager:55000
WAZUH_MANAGER_USERNAME=wazuh-wui
# Default password generated for security. Change this for production.
WAZUH_MANAGER_PASSWORD=ChangeMe_WazuhManager_$(openssl rand -hex 12 )

# --- Graylog Configuration ---
GRAYLOG_URL=http://graylog:9000
GRAYLOG_USERNAME=admin
GRAYLOG_PASSWORD=ChangeMe_GraylogAdmin_$(openssl rand -hex 12 )
# Graylog server password secret (64+ chars). Change this in production.
# Tip to generate: pwgen -N 1 -s 96
PASSWORD_SECRET=ChangeMe_GraylogSecret_6Sk3nQy9r4xS1pB2dT8mV5wX9aC3eRg7uH2jK5mN8pQ2tV5yZ8cB1nM4rT7wY0

# --- Grafana Configuration ---
GRAFANA_URL=http://grafana:3000
GRAFANA_USERNAME=admin
GRAFANA_PASSWORD=ChangeMe_GrafanaAdmin_$(openssl rand -hex 12 )

# --- OpenSearch/Wazuh Indexer Connection Details ---
# These are used by tools that connect directly to the indexer.
# OPENSEARCH_URL=https://wazuh.indexer:9200
# OPENSEARCH_USERNAME=admin
# OPENSEARCH_PASSWORD=${WAZUH_INDEXER_PASSWORD} # Re-uses the indexer password
# OPENSEARCH_SSL_VERIFY=false
# OPENSEARCH_SSL_SHOW_WARN=false
